name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: 22

      - name: Check JavaScript syntax
        run: |
          node --check data.js
          node --check names.js
          node --check script.js
          node --check map.js
          node --check map-layout.js

      - name: Check the quiz data
        run: node scripts/check-quiz.js

      - name: Check assets and element ids
        run: node scripts/check-assets.js

      - name: Check the map still matches the quiz
        run: node scripts/check-map.js
